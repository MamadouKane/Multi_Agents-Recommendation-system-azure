# ---- Secrets  ----
.env
.env.*
!.env.example
*.pem
*.key
*-credentials.json
serviceAccount*.json
local.settings.json

# ---- Azure ----
.azure/
*.azd/

# ---- Python ----
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.ipynb_checkpoints/

# ---- Node / Web ----
node_modules/
.next/
dist/
web-build/
.expo/

# ---- ML / derived data ----
mlruns/
outputs/
*.pkl
data/processed/
data/interim/

# ---- Evaluation reports (keep the baseline) ----
evals/reports/*
!evals/reports/.gitkeep
!evals/reports/baseline.json

# ---- Docs (temporarily untracked: cleanup in progress, re-added at the end of the project) ----
# Exception: the diagram used by the README, otherwise it would not render on GitHub.
# Git does not descend into an excluded directory, so the exclusion is written entry by entry.
docs/*
!docs/assets/
docs/assets/*
!docs/assets/archi-simplified.svg

# ---- OS / IDE ----
.DS_Store
Thumbs.db
.vscode/
.idea/
